[package]
name = "sunrise-sys"
version = "0.1.0"
edition = "2021"
description = "Native transport (reqwest) for the SunriseOBX API client"

[dependencies]
sunrise-common = { workspace = true }
reqwest = { version = "0.12", features = ["json"] }
async-trait = { workspace = true }
tokio = { workspace = true }
